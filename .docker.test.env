# Test-specific environment variables for Docker
# These override values from .env when running tests

POSTGRES_SERVER=test_db
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=test-password
POSTGRES_DB=test_db

SECRET_KEY=test-secret-key-for-testing-only

# OAuth2 test configuration
OAUTH2_JWKS_URL=https://example.com/.well-known/jwks.json
OAUTH2_ISSUER=https://example.com
